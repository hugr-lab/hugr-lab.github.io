---
sidebar_position: 9
title: "DuckLake: NYC Yellow Taxi"
---

This example demonstrates how to use **DuckLake** as a data source in hugr, featuring the NYC Yellow Taxi trip dataset with ~36M real taxi trip records stored in MinIO S3. It showcases self-describing schemas, time-travel queries with the `@at` directive, relationships, DDL operations, and DuckLake management functions.

You can find the complete example in the [Hugr examples repository](https://github.com/hugr-lab/examples).

To read more about the DuckLake data source, see the [DuckLake data source documentation](/docs/engine-configuration/data-sources/ducklake).

:::note Prerequisites
You need the example environment running (`sh scripts/start.sh`), DuckDB CLI installed locally, and MinIO Client (`mc`) for bucket management.
:::

## Example

import RemoteMarkdown from '@site/src/components/RemoteMarkdown';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<Tabs>
  <TabItem value="readme" label="Getting Started">
    <RemoteMarkdown
      url="https://raw.githubusercontent.com/hugr-lab/examples/main/examples/ducklake/README.md"
      fallback="Loading documentation..."
    />
  </TabItem>

  <TabItem value="schema" label="Extension Schema">
    <RemoteMarkdown
      url="https://raw.githubusercontent.com/hugr-lab/examples/main/examples/ducklake/schema.graphql"
      fallback="Loading schema..."
      transform={(content) => '```graphql\n' + content + '\n```'}
    />
  </TabItem>

  <TabItem value="setup" label="Setup Script">
    <RemoteMarkdown
      url="https://raw.githubusercontent.com/hugr-lab/examples/main/examples/ducklake/setup.sh"
      fallback="Loading script..."
      transform={(content) => '```bash\n' + content + '\n```'}
    />
  </TabItem>
</Tabs>
